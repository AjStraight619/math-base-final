import { FolderWithLastNote } from "@/lib/types";

type NoteDisplayProps = {
  folders: FolderWithLastNote;
};

const NoteDisplay = ({ folders }: NoteDisplayProps) => {
  return <div>NoteDisplay</div>;
};

export default NoteDisplay;
